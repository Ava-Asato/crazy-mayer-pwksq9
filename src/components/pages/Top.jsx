import { styled } from "styled-components";
export const Top = () => {
  return (
    <SContainer>
      <h2>ユーザー一覧です</h2>
    </SContainer>
  );
};

const SContainer = styled.div`
  text-align: center;
`;
